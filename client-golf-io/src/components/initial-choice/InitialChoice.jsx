import React from 'react';
import './InitialChoice.css';

function InitialChoice({ socket, state, setState }) {
    return (
        <div className='InitialChoice'>
            choice
        </div>
    );
}

export default InitialChoice;