import React from 'react';

function Slot({ socket, state, setState, slot }) {
    console.log(slot);
    return (
        <div className='Slot'>
            slot
        </div>
    );
}

export default Slot;